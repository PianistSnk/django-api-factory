# M1 T1.1 范围清单 — strip project-specific business coupling

**目标：** 把新 src/django_api_factory/ 里残留的 project-specific business assumptions剥干净，让它能run independently of any project-specific packages。
**用户原始工时估：** 16h（基于老 admin.py 23K 全剥；M0 已剥了大部分，T1.1 实际范围大幅缩小）
**实际盘点日期：** 2026-06-05 凌晨 04:25 (Asia/Shanghai)

---

## 当前状态（M0 已剥的）

| 残留类别 | 老 admin.py 位置 | 状态 |
|---|---|---|
| `from legacy.changelist/filter/queryset import *` | Line 20-22 | ✅ M0 剥完 |
| `from legacy.newrequests import requests` | Line 23 | ✅ M0 剥完（直接用 `import requests`）|
| `from legacy.settings import REDIS_HOST, REDIS_PWD` | Line 24 | ✅ M0 剥完（改用 `django.conf.settings`）|
| `class BaseAjaxAdmin(AjaxAdmin)` | Line 533-553 | ✅ M0 剥完 |
| `class YYDMAdmin(BaseAdmin)` | Line 556-573 | ✅ M0 剥完 |

**After M0: 0 imports of project-specific packages in src/django_api_factory/admin.py (328 lines).**

---

## T1.1 真正剩余范围（3 个业务假设残留）

### 1. 顿号搜索语义（业务假设）
- **位置：** src/django_api_factory/admin.py L184-L191 (`handle_search_condition` 函数)
- **现状：**
  ```python
  def handle_search_condition(item_value, search_terms):
      if len(search_terms) == 1 and "、" not in search_terms[0]:
          return search_terms[0] in item_value
      return all(
          field_name in item
          and "、".join(sorted(str(item[field_name]).split("、"))) in paras[field_name].split(",")
          for field_name in paras
      )
  ```
- **问题：** Hard-coded assumption that "multi-value separator is 顿号 (、)" — project-specific data format.其他项目可能用 `,` / `;` / `|`。
- **剥法：** 抽到 `SearchSplitter` strategy 类，可子类化换默认分隔符。Default remains `、` (backward compatible with legacy data).
- **估时：** 2-3h（含测试）

### 2. LogEntry 查询审计（业务审计）
- **位置：** src/django_api_factory/admin.py L222-L231 (get_api_data 末尾) + L37-L48 (view_or_download)
- **现状：**
  ```python
  LogEntry = _get_logentry_model()
  if LogEntry:
      try:
          LogEntry.objects.create(
              action_time=datetime.datetime.now(),
              user=request.user,
              action_flag=4,  # ← 4 不在 Django admin 标准 flag (1=add, 2=change, 3=delete) 里
              content_type_id=ContentType.objects.get(model=self.model.__name__).id,
          )
      except Exception:
          pass
  ```
- **问题：** `action_flag=4` is a custom flag, **not in Django admin's standard list** (1/2/3). This was a hard-coded business audit assumption.
- **剥法：** 抽到 `AuditLogMixin`，默认 no-op；用户在 settings 或子类里注册 callback 即可。
- **估时：** 2h（含测试）

### 3. LogEntry 下载审计（业务审计）
- **位置：** src/django_api_factory/admin.py L37-L48 (view_or_download)
- **现状：**
  ```python
  if filename:
      ...
      LogEntry = _get_logentry_model()
      if LogEntry:
          LogEntry.objects.create(
              action_time=datetime.datetime.now(),
              user=user,
              action_flag=6,  # ← 6 同样不是标准 flag
              object_repr=f"{filename}.{type_}",
              content_type=ContentType.objects.get(model=name),
          )
  ```
- **问题：** 同上，`action_flag=6` is a custom download audit flag.
- **剥法：** 与 #2 合并到同一个 `AuditLogMixin`（不同 callback name）。
- **估时：** 已包含在 #2 里

---

## 任务分解

| 子任务 | 估时 | 依赖 | 说明 |
|---|---|---|---|
| T1.1a | 剥 LogEntry 审计 → AuditLogMixin (no-op 默认) | 2h | 无 | **今晚做这个 spike** |
| T1.1b | 剥顿号搜索 → SearchSplitter strategy | 2-3h | T1.1a 后 | 默认顿号，可子类化 |
| T1.1c | 测试覆盖（两个 mixin/strategy） | 2h | T1.1a + T1.1b | pytest |
| T1.1d | 文档（README 更新） | 1h | T1.1c 后 | 怎么换默认分隔符 / 注册审计 callback |

**T1.1 实际工时估：** 7-8h（比用户原始估的 16h 少一半 — M0 已剥大头）

---

## 完整 T1.1 范围（5 项）

| ID | 任务 | 估时 | 状态 |
|---|---|---|---|
| T1.1a | 剥 LogEntry 审计 → AuditLogMixin | 2h | 🟡 今晚做 |
| T1.1b | 剥顿号搜索 → SearchSplitter strategy | 2-3h | ⏸️ |
| T1.1c | 测试覆盖 | 2h | ⏸️ |
| T1.1d | 文档更新 | 1h | ⏸️ |
| T1.1e | （预留 — 如有发现） | — | — |

**T1.1 总计：7-8h（用户原始估 16h）**

---

## T1.1 之后（T1.2-T1.5 简述，等 T1.1 完成后展开）

- **T1.2 (12h)** 解耦 simpleui/Redis → backend 插件模式
- **T1.3 (12h)** Schema cache 机制（注册一次，复用）
- **T1.4 (8h)** 修复 add_to_class 并发坑（替代为 Schema 注册）
- **T1.5 (12h)** 重写测试套件（pytest + 覆盖率）

**M1 全部工时估：~50h**（用户原始估 80h — M0 简化了部分工作）
