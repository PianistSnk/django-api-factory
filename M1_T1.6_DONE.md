# M1 T1.6 Filter UX — 完整发版记录 (2026-06-06 → 2026-06-07)

> **T1.6 任务**: 把 `APIFilter` 升级成"单选/多选可混用" + 本地搜索候选项 + 沿用 simpleui 风格的「确定/清空」批量应用按钮 + 互斥 dropdown。**让 `list_filter = [...]` 列出几个字段就是几个 filter, 每个字段自己选 APIFilter / APIMultiSelectFilter**。
>
> **原始工时估**: M1 计划里没列 (新增任务, 用户 4 个核心需求里 #2 的延伸)
> **实际工时**: ~12h (从 2026-06-06 凌晨 04:25 用户首次报 "全乱" bug 开始, 到 2026-06-07 22:00 T1.6.7 删注释收尾)

---

## 完整版本链 (8 个子任务)

| T 编号 | 实际工时 | 状态 | 改动总结 |
|---|---|---|---|
| T1.6.0 | ~1.5h | ✅ | 引入 `APIMultiSelectFilter` + filter.html 加 `{% if spec.is_multi %}` 分支 (checkbox + 「确定/清空」) + 互斥 `<details name="filter-inline-group">` |
| T1.6.1 | ~0.2h | ✅ | 删 filter 内 search input (用户反馈 "el-autocomplete 思路没用") |
| T1.6.2 | ~0.5h | ✅ | 加回 input (关联 changelist form + 按 Enter 跳 `?q=xxx` 搜全表) — **方向跑偏** |
| T1.6.3 | ~0.3h | ✅ | 改回 T1.6.0 思路: input 输 `T` 本地 hide/show `<li>`, **不**跳 URL (跟 `MultiSelectDropdownFilter` UX 一致) |
| T1.6.4 | ~0.2h | ✅ | **修 Django 4.x admin 没 `{% block extrascript %}` 的静默丢 JS bug** — 改用 `{% block extrabody %}` |
| T1.6.5 | ~0.5h | ✅ | 修顶部 search bar: `all(...) → any(...)` + `.lower()` 双边归一化 (`?q=graham` 之前返 0 行) |
| T1.6.6 | ~0.1h | ✅ | PostAdmin 演示: `list_filter = [(userId, APIFilter), (title, APIFilter), (body, APIMultiSelectFilter)]` (1 多 + 2 单) |
| T1.6.7 | ~0.1h | ✅ | 删 3 段残留的 `{# #}` 多行注释 (T1.6.0 留下来的) — **第 6 次踩同一个坑**, 升级到 SOUL.md hard rule |

实际工时比估的少很多是因为 **T1.6.0 写对了**但 `extrascript` 块丢了**静默 7 小时**, 期间反复重写 3 次方向都跑偏 (T1.6.1 → T1.6.2 → T1.6.3), 主要是 debug 时间, 不是设计时间。

---

## 8 个子任务详细记录

### T1.6.0 — 引入 APIMultiSelectFilter + 互斥 dropdown (2026-06-07 04:55)

**触发**: 用户 "MultiSelectDropdownFilter 是用户可自定义的东西" (5:25 说"偶尔添加个轮子也合适"), 想要单/多选按字段配 + 沿用 simpleui 风格「确定」按钮

**新增文件**:
- 无 (新加 filter class 写到现有 `filter.py`)

**修改文件**:
- `src/django_api_factory/filter.py`:
  - `APIFilter.is_multi = False` (class attr, template 读 `{% if spec.is_multi %}` 切分支)
  - `APIFilter.choices()` override: 每个 choice dict 加 `value` 字段 (给 checkbox 的 `data-value=`)
  - 新加 `class APIMultiSelectFilter(APIFilter): is_multi = True` + 复用 `choices()`
- `src/django_api_factory/templates/admin/django_api_factory/filter.html`:
  - `<details name="filter-inline-group" class="filter-inline ...">` 互斥 dropdown (HTML5 原生)
  - `{% if spec.is_multi %}` 分支: checkbox + `<label>` + 底部「确定」「清空」按钮
  - `{% else %}` 单选分支: 保持 `<a href="?field=v">`
  - `.filter-inline-multi` CSS 浅蓝 pill 视觉区分
- `src/django_api_factory/templates/admin/django_api_factory/change_list.html`:
  - **❌ 用 `{% block extrascript %}` (Django 4.x 没这个 block)**
  - 「确定」「清空」JS (事件委托 + `buildUrl` 收集 checkbox data-value 拼 `?field=v1,v2,v3`)
- `src/django_api_factory/admin.py`:
  - `get_list_filter` 改: `if self.list_filter: return self.list_filter` — 用户 list_filter 优先, fallback 全字段 APIFilter
- `example/api/admin.py`:
  - `UserAdmin.list_filter = [('name', APIMultiSelectFilter), ('username', APIFilter), ('email', APIMultiSelectFilter)]` 演示 (1 多 + 2 单)

**端到端验证**:
- `?name=Leanne+Graham,Clementine+Bauch` → 2 行 ✓
- 单选 `<a href>` 立即跳 ✓
- 多选 checkbox 渲染 22 个 ✓

**❌ JS 全丢**: `{% block extrascript %}` 在 Django 4.x admin change_list.html **不存在**, 整个块静默丢, 所有 JS 都没渲染 — 但**当时没人知道**

---

### T1.6.1 — 删 filter 内 search input (2026-06-07 11:51)

**触发**: 用户报 "现在的搜索框是有输入功能, 但完全没有任何用处" (14:30 截图 filter 内输 `est` 啥也没发生)

**误解**: 我以为用户要 "搜全表" → 决定**直接删 input** (砍掉 el-autocomplete 思路)

**修改文件**:
- `filter.html`: 删 `<input class="filter-inline-search">` + 相关 CSS
- `change_list.html` extrascript: 删 input 事件 + hide/show li JS + Esc 清空 JS

**❌ 误解加深**: 用户要的是 "在 filter 内搜索, 过滤该 filter 候选项" (跟 `MultiSelectDropdownFilter` UX), 我误读成 "不要 input" — 删错了

---

### T1.6.2 — 加回 input (按 Enter 跳全表) (2026-06-07 17:50)

**触发**: 用户 "输入框不识别了" (他看到我删了 input)

**误解**: 我以为用户要 "input 输完按 Enter 跳 `?q=xxx` 过滤全表"

**修改文件**:
- `filter.html` 加回 `<input name="q" form="changelist-search" placeholder="搜全表 (按 Enter)...">`
- `filter.py` 加 `self._current_q = request.GET.get("q", "")` 给 template 回填
- (踩坑: 模板变量名不能 `_` 开头, 改 `q_value`)

**❌ 误解+1**: 跟 T1.6.1 一样, 又把 "filter 候选项过滤" 误解成 "全表 search"

---

### T1.6.3 — 改回 T1.6.0 思路 (本地 hide/show 候选项) (2026-06-07 21:21)

**触发**: 用户明确说 "我在筛选器里筛选 不能放在 url 上啊 应该是在每个筛选器里筛选啊 类似于MultiSelectDropdownFilter"

**修复**: input 纯本地 hide/show `<li>`, **不**跳 URL, **不**关联 changelist form

**修改文件**:
- `filter.html`: 删除 `name="q" form="changelist-search"`, 删除 `value` 回填, placeholder 改 "搜索 {{ title }}..."
- `filter.py`: 删 `self._current_q` / `self.q_value`
- `change_list.html` extrabody: input 事件 → hide/show li (T1.6.0 那套 JS), Esc 清空 + 显示全部

**✅ 终于对**: UX 跟 `MultiSelectDropdownFilter` 一致 — 输入时只过滤候选项, 选了才跳 URL

---

### T1.6.4 — 修 extrascript 块静默丢 JS (2026-06-07 19:30)

**触发**: 用户报 "输入框不识别" (T1.6.2 那次) — 排查发现 `setTimeout` / `navToQuery` / `Escape` 字符串**在渲染的 HTML 里 0 次**

**根因** (关键发现):
- Django 4.x `change_list.html` **没** `{% block extrascript %}` block
- Django 静默丢整个 block 内容, 不报错不警告
- **之前 T1.6.0 写的「确定/清空」JS + T1.6.1 写的 hide/show li JS + T1.6.2 写的 debounce JS 全部丢了 7 小时**

**修复**:
- `change_list.html`: 改用 `{% block extrabody %}` (Django 4.x 有, 注入到 `</body>` 之前)

**验证**:
- `setTimeout=1, navToQuery=3, Escape=1, addEventListener=5, filter-inline-apply=4, filter-inline-check=1` — **全渲染**

**教训**: **改任何 block 之前先 `grep "block " <父模板路径>` 看实际支持哪些 block**

---

### T1.6.5 — 修顶部 search bar `all→any` + 大小写归一化 (2026-06-07 10:15)

**触发**: 用户报 "顶部 search 框试了不行" (实际上是 T1.6.0 那次，但 10:15 我以为是顶部 search bar 问题)

**根因**:
- `get_api_data` 第 729 行 `all(handle_search_condition(...))` 应为 `any(...)` — 任一字段含 q 就收
- `in` 大小写敏感 — `?q=graham` miss "Leanne Graham" (大写 G)

**修复**:
```python
q_lc = q.lower()
field_matches = any(
    q_lc in str(v).lower()
    for v in item.values() if isinstance(v, str)
)
```

**验证**:
- `?q=graham`: 0 → 1 ✓
- `?q=Leanne`: 1 ✓
- `?q=LEANNE` / `?q=LeAnNe` 大小写不敏感 ✓

---

### T1.6.6 — PostAdmin 演示: 1 多选 + 2 单选 (2026-06-07 21:30)

**触发**: 用户 "可以把 body 改成多选吗?"

**修改文件**:
- `example/api/admin.py`:
  ```python
  class PostAdmin(APIAdmin):
      list_filter = [
          ("userId", APIFilter),
          ("title", APIFilter),
          ("body", APIMultiSelectFilter),
      ]
  ```

**验证**:
- `?body=A,B` → 2 行 ✓
- 101 个 body checkbox + 「确定/清空」按钮 ✓
- userId / title 单选, 11 / 101 个 `<a href>` 候选 ✓

---

### T1.6.7 — 删 3 段残留的 `{# #}` 多行注释 (2026-06-07 21:44)

**触发**: 用户截图 — body 多选框里**每个候选项都重复显示** `{# Multi-select: checkbox + label. JS in change_list.html collects ... #}` 整段文字

**根因**: T1.6.0 写的 3 段多行 `{# #}` 注释残留 (Django 单行注释, 多行原样输出)

**修复**: 删 filter.html 第 18-20 / 28 / 35-37 行所有多行 `{# #}` 注释

**升级 SOUL.md**: 第 6 次踩同一个坑, 升级到 super hard rule (top of Hard Rules)
- "**任何时候都不要写 `{# `** — 包括单行也别写 — 写到 commit message / memory / 直接删"
- "用 `{% comment %}` 替代 (多行 OK, 不会渲染)"

---

## T1.6 累计改动文件清单

### 修改
- `src/django_api_factory/filter.py` (~30 行净增): APIMultiSelectFilter + choices() override + is_multi class attr
- `src/django_api_factory/admin.py` (~10 行净增): get_list_filter 优先级 + all→any 修 search + import APIMultiSelectFilter
- `src/django_api_factory/templates/admin/django_api_factory/filter.html` (60 行重写): 互斥 details + 多/单选 UI + search input + 「确定/清空」按钮 + CSS
- `src/django_api_factory/templates/admin/django_api_factory/change_list.html` (~50 行净增): extrabody 块注入 JS (互斥/确定清空/搜索 hide-show/单选跳转)
- `example/api/admin.py` (~15 行净增): UserAdmin + PostAdmin list_filter 配置
- `example/example/settings.py` (~10 行净增): 显式禁 cached.Loader (一劳永逸改模板不需 restart)
- `SOUL.md`: 加 4 条 hard rules (`{# #}` 注释 / block 名 / JS debug / OCR 截图)

### 新增测试
- 无 (T1.5 还没做)

---

## T1.6 范围内未做的事 (留给后续 T)

- ❌ T1.5 重写测试套件 (T1.6 的 8 个子任务**完全没单测** — 全部靠手动 curl + 浏览器验证)
- ❌ 100+ 候选项的滚动性能 (现在 max-height: 240px + overflow-y: auto, 没性能问题)
- ❌ keyboard navigation (↑↓ 选 + Space toggle, Tab 跳)
- ❌ remote search (输入时 ajax 重拉数据, 现在 100% 本地)
- ❌ 「全选/全不选」按钮 (multi-select 现在只能一个个勾)

---

## 给明早的 hook

- **M1 整体进度**: T1.1 ✅ / T1.2 ⏳ (今天部分) / T1.3 ⏸️ / T1.4 ⏸️ / T1.5 ⏸️ / T1.5b ✅ / T1.6 ✅
- **T1.6.5 的 search bug** 是真的改对了 (all→any + case-insensitive), 复测一下 User 表 + Post 表都 work
- **T1.6.4 的 extrabody 块** 是关键修复, 之前 7 小时 JS 全丢 (T1.6.0/T1.6.1/T1.6.2 累计)
- **SOUL.md hard rules** 已经永久记录 4 条 — 下次 session 启动时会看到
- 下一步选项:
  1. **T1.5 重写测试套件** (给 T1.6 加单测 — 现在 0 测, 全靠手动)
  2. **T1.2 simpleui/Redis 解耦** (M1 计划里的 12h 任务, 没做)
  3. **T1.3 Schema cache** (12h, 没做)
  4. **T1.4 add_to_class 并发** (8h, 没做)
  5. **M2 服务端分页** (T2.1 ✅ / T2.2 ✅, 跟 M1 并行)
